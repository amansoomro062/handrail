{
  "name": "railing",
  "version": "0.1.0",
  "private": true,
  "description": "Continuous, public accessibility conformance testing for UI component libraries.",
  "license": "MIT",
  "engines": {
    "node": ">=20.11"
  },
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "adapter:radix": "pnpm --filter @railing-dev/adapter-radix run dev",
    "announce": "pnpm --filter @railing-dev/announce run cli --",
    "build": "pnpm -r --filter \"./packages/**\" run build",
    "calibrate": "pnpm railing run --target _fixture-broken --component dialog --base-url http://localhost:5199 --expect adapters/_fixture-broken/expected.json",
    "check:site": "node scripts/audit-site.mjs",
    "check:versions": "node scripts/check-versions.mjs",
    "clean": "rm -rf packages/*/dist adapters/*/dist node_modules/.cache results",
    "fixture:broken": "pnpm --filter @railing-dev/fixture-broken run dev",
    "railing": "pnpm --filter @railing-dev/runner run cli --",
    "notify:bundle": "node scripts/notify-bundle.mjs",
    "restore:docs": "node scripts/restore-docs.mjs",
    "site:build": "pnpm --filter @railing-dev/web run build",
    "site:dev": "pnpm --filter @railing-dev/web run dev",
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "playwright": "catalog:",
    "typescript": "catalog:",
    "yaml": "catalog:"
  }
}
