{
  "name": "@curbcut/fixture-broken",
  "version": "0.1.0",
  "description": "A deliberately broken dialog with catalogued defects. Makes the runner's false-positive and false-negative rate a measured number rather than an assumption.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5199 --strictPort",
    "preview": "vite preview --port 5199 --strictPort",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
