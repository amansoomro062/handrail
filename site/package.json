{
  "name": "@handrail/site",
  "version": "0.1.0",
  "description": "Static site generator for the public index. Reads results/*.json and emits HTML, badges and raw JSON.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx src/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@handrail/report": "workspace:*",
    "@handrail/spec": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
