node_modules/
dist/
build/
.DS_Store
*.log

# Runner output
# results/ is deliberately committed. It is the data the index is built from, so
# a clone can build the site without a ten minute measurement, and each commit
# becomes a dated snapshot that makes score history diffable.
test-results/
playwright-report/
traces/

# Env
.env
.env.local

# Editor
.vscode/
.idea/

# Generated site
site/dist/
site/dist-ts/
